# PDF 导出

将 Book Lens 生成的 Markdown 笔记导出为 PDF。

可选源文件通常包括：

```text
book-notes/<name>/
├── speed-read.md
├── book-reading.md
├── reading-notes.md
└── slides-content.md
```

优先使用本 skill 下的 `scripts/md_to_pdf.py`。如果脚本不可用，说明需要安装依赖或手动导出，不要伪造已生成文件。
